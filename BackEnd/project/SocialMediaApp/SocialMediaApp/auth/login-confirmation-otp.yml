info:
  name: login-confirmation-otp
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/auth/login/confirmation-otp
  body:
    type: json
    data: |-
      {
        "email": "meellsayed@gmail.com",
        "otp": "59672"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
