info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/auth/login
  body:
    type: json
    data: |-
      {
        "email": "meellsayed@gmail.com",
        "password": "nMnM@#15790"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
